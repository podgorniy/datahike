#!/usr/bin/env bash

function on_path {
    builtin type -P "$1" &> /dev/null
}

GRAAL_NOT_ON_PATH="PATH does not contain native-image. Make sure to add your GraalVM to it."
on_path native-image && clojure -A:native-image --no-fallback || echo $GRAAL_NOT_ON_PATH
